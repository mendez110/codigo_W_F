/*Cree un programa que imprima la tabla de multiplicar del 3, del 0 al 50. 
Use ciclo for*/

for (let index = 0; index <= 50; index++) {
    x = index * 3;
    console.log("3 x "+ index +" : "+ x);
}
